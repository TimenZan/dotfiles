import { RenameParams, WorkspaceEdit, Range, TextDocumentPositionParams } from 'vscode-languageserver';
export declare const prepareProvider: (params: TextDocumentPositionParams) => {
    range: Range;
    placeholder: string;
};
export declare const renameProvider: (params: RenameParams) => WorkspaceEdit;

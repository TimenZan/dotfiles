import { Node } from '../../lib/vimparser';
import { CompletionItem, Location, Position } from 'vscode-languageserver';
export declare class Workspace {
    private buffers;
    private filterDuplicate;
    private getGlobalFunctionItems;
    private getScriptFunctionItems;
    private getGlobalIdentifierItems;
    private getLocalIdentifierItems;
    private getLocation;
    private getGlobalLocaltion;
    private getScriptLocation;
    private getLocalLocation;
    private getAIdentifierLocation;
    private getFunArgLocation;
    isExistsBuffer(uri: string): boolean;
    updateBuffer(uri: string, node: Node): Promise<void>;
    getFunctionItems(uri: string): CompletionItem[];
    getIdentifierItems(uri: string, line: number): CompletionItem[];
    getLocations(name: string, uri: string, position: Position, locationType: 'definition' | 'references'): {
        locations: Location[];
        isFunArg: boolean;
    };
}
export declare const workspace: Workspace;
